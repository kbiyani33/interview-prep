from typing import List
from math import inf
class Solution:
    def recursiveSolution(self, grid: List[List[int]], moveCost: List[List[int]], prow:int, pcol:int) -> int:
        m, n = len(grid), len(grid[0])
        
    def minPathCost(self, grid: List[List[int]], moveCost: List[List[int]]) -> int:
        return self.recursiveSolution(grid, moveCost, len(grid), len(grid[0]))